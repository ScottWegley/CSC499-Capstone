import { browser } from "$app/environment";
import { getSpecificWordlist } from "$lib/scripts/Util/Dictionary";

export const prerender = true;


/** @type {import('./$types').LayoutLoad} */
export async function load() {
    
}